package core

// type OrderSet map[common.Hash]*zeroex.SignedOrder

// func NewOrderSet() OrderSet {
// 	return OrderSet(map[common.Hash]*zeroex.SignedOrder{})
// }

// func (s OrderSet) Add(order *zeroex.SignedOrder) {
// 	hash, err := order.ComputeOrderHash()
// 	if err != nil {
// 		log.WithField("error", err).Panic("Could not compute order hash")
// 	}
// 	s[hash] = order
// }

// func (s OrderSet)
